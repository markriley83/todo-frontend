import React, {FunctionComponent} from 'react';

const Header: FunctionComponent = () => {
    return (
        <header>
            <h1>Todo List</h1>
        </header>
    );
};

export default Header;